# Use npm's canonical registry. Package integrity remains locked in package-lock.json.
# Keep concurrency bounded: some mirrors/proxies accept many keep-alive sockets but
# stop delivering package bodies, which previously left release builds hung forever.
registry=https://registry.npmjs.org/
maxsockets=10
progress=false
fetch-retries=3
fetch-timeout=120000
